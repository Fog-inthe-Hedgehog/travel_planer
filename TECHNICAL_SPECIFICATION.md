# ТЕХНИЧЕСКОЕ ЗАДАНИЕ
## Telegram-бот "Travel Planner" (Планировщик путешествий)

---

## 1. ОБЩЕЕ ОПИСАНИЕ ПРОЕКТА

### 1.1. Назначение системы
Telegram-бот "Travel Planner" — это асинхронное приложение для планирования путешествий, которое позволяет пользователям:
- Создавать и управлять поездками
- Добавлять задачи к поездкам
- Получать информацию о погоде в пунктах назначения
- Находить достопримечательности в городах

### 1.2. Целевая аудитория
Пользователи Telegram, планирующие путешествия и нуждающиеся в организации поездок.

### 1.3. Платформа
Telegram Bot API (через библиотеку aiogram 3.x)

---

## 2. ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

### 2.1. Управление пользователями

**FR-1.1. Регистрация пользователя**
- При первом использовании команды `/start` пользователь автоматически регистрируется в системе
- Сохраняются данные: `user_id`, `username`, `first_name`, `registration_date`
- Если пользователь уже существует, повторная регистрация не выполняется

### 2.2. Управление поездками

**FR-2.1. Создание поездки**
- Команда: `/new_trip`
- Многошаговый процесс создания через FSM (Finite State Machine):
  1. Ввод направления (город или страна) — минимум 2 символа
  2. Ввод даты начала поездки (формат: ДД.ММ.ГГГГ)
  3. Ввод даты окончания поездки (формат: ДД.ММ.ГГГГ)
  4. Ввод заметок (опционально, можно отправить "-" для пропуска)
- Валидация:
  - Дата окончания должна быть позже или равна дате начала
  - Формат даты строго ДД.ММ.ГГГГ
- Возможность отмены процесса командой `/cancel`

**FR-2.2. Просмотр поездок**
- Команда: `/my_trips`
- Отображение списка всех поездок пользователя
- Сортировка по дате начала (от новых к старым)
- Каждая поездка отображается с кнопками:
  - Просмотр поездки
  - Просмотр задач поездки
  - Удаление поездки

**FR-2.3. Удаление поездки**
- Удаление через inline-кнопку в списке поездок
- Подтверждение удаления

### 2.3. Управление задачами

**FR-3.1. Добавление задачи**
- Команда: `/add_task`
- Процесс:
  1. Выбор поездки из списка (reply-клавиатура)
  2. Ввод описания задачи
- Задача привязывается к конкретной поездке

**FR-3.2. Просмотр задач**
- Команда: `/tasks`
- Отображение задач по всем поездкам пользователя
- Группировка задач по поездкам
- Каждая задача отображается с:
  - Статусом выполнения (✅ выполнено / ⬜ не выполнено)
  - Кнопкой переключения статуса
  - Кнопкой удаления

**FR-3.3. Управление статусом задачи**
- Переключение статуса выполнения через inline-кнопку
- Визуальная индикация статуса

**FR-3.4. Удаление задачи**
- Удаление через inline-кнопку
- Удаление из базы данных

### 2.4. Информация о погоде

**FR-4.1. Текущая погода**
- Команда: `/weather [город]`
- Если город не указан:
  - Предлагается выбрать город из списка поездок пользователя
  - Возможность ввести другой город
- Отображаемые данные:
  - Температура (°C)
  - Описание погодных условий
  - Влажность (%)
  - Скорость ветра (м/с)
- Кэширование результатов на 10 минут (600 секунд)

**FR-4.2. Прогноз погоды**
- Команда: `/forecast [город]`
- Если город не указан:
  - Предлагается выбрать город из списка поездок пользователя
  - Возможность ввести другой город
- Прогноз на 5 дней
- Для каждого дня отображается:
  - Дата (формат: ДД.ММ.ГГГГ)
  - Температура (°C)
  - Описание погодных условий
  - Влажность (%)
  - Скорость ветра (м/с)
- Прогноз берется на 12:00 (полдень) каждого дня
- Кэширование результатов на 10 минут (600 секунд)

### 2.5. Достопримечательности

**FR-5.1. Поиск достопримечательностей**
- Команда: `/top_location [город]`
- Если город не указан:
  - Предлагается выбрать город из списка поездок пользователя
  - Возможность ввести другой город
- Отображается список достопримечательностей (по умолчанию 5)
- Для каждой достопримечательности:
  - Название
  - Тип/категория
  - Рейтинг (если доступен)
- Кэширование результатов на 30 минут (1800 секунд)

**FR-5.2. Источники данных (приоритет)**
1. **Foursquare API v3** (основной источник)
   - Поиск туристических достопримечательностей
   - Максимум 50 результатов за запрос
   - Возвращает название, категорию, рейтинг

2. **OpenTripMap API** (резервный источник)
   - Геокодирование города для получения координат
   - Поиск интересных мест в радиусе 10 км
   - Возвращает название и тип

3. **DeepSeek API** (резервный источник через LLM)
   - Используется, если внешние API недоступны
   - Генерирует список достопримечательностей через языковую модель
   - Возвращает название, адрес, описание

4. **Mock данные** (fallback)
   - Используются, если все API недоступны
   - Предопределенный список типовых достопримечательностей

### 2.6. Общие функции

**FR-6.1. Отмена действий**
- Команда: `/cancel`
- Отменяет текущий процесс создания поездки или задачи
- Очищает состояние FSM

**FR-6.2. Обработка ошибок**
- Глобальный обработчик ошибок
- Информативные сообщения об ошибках для пользователя
- Логирование ошибок в систему

---

## 3. ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ

### 3.1. Технологический стек

**Backend:**
- Python 3.9+
- aiogram 3.x (асинхронный фреймворк для Telegram Bot API)
- SQLAlchemy 2.0+ (ORM для работы с БД)
- Alembic (миграции БД)
- aiohttp (асинхронные HTTP-запросы)
- python-dotenv (управление переменными окружения)
- pydantic 2.0+ (валидация данных)
- openai 2.8+ (для работы с DeepSeek API)

**База данных:**
- PostgreSQL 13+
- Драйвер: psycopg2-binary

**Инфраструктура:**
- Docker & Docker Compose (для развертывания БД)
- Poetry (управление зависимостями)

### 3.2. Архитектура системы

**Паттерн проектирования:** Многослойная архитектура (Layered Architecture)

**Слои:**
1. **Handlers** (Обработчики) — обработка команд и сообщений от пользователей
2. **Services** (Сервисы) — бизнес-логика, интеграции с внешними API
3. **Repositories** (Репозитории) — работа с базой данных
4. **Models** (Модели) — модели данных SQLAlchemy
5. **Utils** (Утилиты) — вспомогательные функции (валидация, форматирование, состояния FSM)
6. **Keyboards** (Клавиатуры) — построение клавиатур для интерфейса

### 3.3. Структура проекта

```
travel_planer/
├── main.py                          # Точка входа приложения
├── alembic/                         # Миграции БД
│   ├── env.py
│   └── versions/
├── app/
│   ├── config.py                    # Конфигурация и настройки
│   ├── database/                    # Работа с БД
│   │   ├── base.py                  # Базовый класс моделей
│   │   ├── models.py                # Модели данных
│   │   └── session.py               # Управление сессиями БД
│   ├── handlers/                    # Обработчики команд
│   │   ├── start.py                 # Стартовый обработчик
│   │   ├── trips.py                 # Обработчики поездок
│   │   ├── tasks.py                 # Обработчики задач
│   │   ├── weather.py               # Обработчики погоды
│   │   ├── points_of_interest.py    # Обработчики достопримечательностей
│   │   └── common.py                # Общие обработчики
│   ├── services/                    # Бизнес-логика
│   │   ├── weather.py               # Сервис погоды
│   │   ├── points_of_interest.py    # Сервис достопримечательностей
│   │   └── validators.py            # Валидация данных
│   ├── repositories/                # Репозитории для работы с БД
│   │   ├── trips.py                 # Репозиторий поездок
│   │   ├── tasks.py                 # Репозиторий задач
│   │   └── users.py                 # Репозиторий пользователей
│   ├── utils/                       # Утилиты
│   │   ├── states.py                # Состояния FSM
│   │   └── formatters.py            # Форматирование ответов
│   ├── keyboards/                   # Клавиатуры
│   │   ├── inline.py                # Inline-клавиатуры
│   │   └── reply.py                 # Reply-клавиатуры
│   └── schemas/                     # Pydantic схемы (опционально)
├── docker-compose.yml               # Конфигурация Docker
├── pyproject.toml                   # Зависимости Poetry
└── .env                             # Переменные окружения
```

### 3.4. База данных

**Схема БД:**

**Таблица `users`:**
- `user_id` (INTEGER, PRIMARY KEY) — ID пользователя Telegram
- `username` (VARCHAR(100)) — имя пользователя Telegram
- `first_name` (VARCHAR(100)) — имя пользователя
- `registration_date` (DATETIME) — дата регистрации

**Таблица `trips`:**
- `trip_id` (INTEGER, PRIMARY KEY, AUTO_INCREMENT) — ID поездки
- `user_id` (INTEGER, FOREIGN KEY → users.user_id) — владелец поездки
- `destination` (VARCHAR(200), NOT NULL) — направление (город/страна)
- `start_date` (DATETIME, NOT NULL) — дата начала
- `end_date` (DATETIME, NOT NULL) — дата окончания
- `notes` (TEXT) — заметки к поездке

**Таблица `tasks`:**
- `task_id` (INTEGER, PRIMARY KEY, AUTO_INCREMENT) — ID задачи
- `trip_id` (INTEGER, FOREIGN KEY → trips.trip_id) — связанная поездка
- `description` (TEXT, NOT NULL) — описание задачи
- `is_completed` (BOOLEAN, DEFAULT FALSE) — статус выполнения

### 3.5. Внешние API

**OpenWeatherMap API:**
- Endpoint: `http://api.openweathermap.org/data/2.5`
- Методы:
  - `/weather` — текущая погода
  - `/forecast` — прогноз погоды
- Параметры: `q` (город), `appid` (API ключ), `units=metric`, `lang=ru`
- Кэширование: 10 минут

**Foursquare API v3:**
- Endpoint: `https://api.foursquare.com/v3/places/search`
- Метод: GET
- Headers: `Authorization: fsq3{API_KEY}`
- Параметры: `query=tourist attractions`, `near` (город), `limit`
- Кэширование: 30 минут

**OpenTripMap API:**
- Endpoints:
  - `/0.1/en/places/geoname` — геокодирование
  - `/0.1/en/places/radius` — поиск POI
- Параметры: `name`, `apikey`, `radius=10000`, `kinds=interesting_places`
- Кэширование: 30 минут

**DeepSeek API:**
- Endpoint: `https://api.deepseek.com` (настраиваемый)
- Модель: `deepseek-chat`
- Формат ответа: JSON-массив с полями `name`, `address`, `description`
- Кэширование: 30 минут

**Amadeus API:**
- Endpoint: `https://test.api.amadeus.com/v1`
- Используется для получения OAuth токена (в текущей реализации не используется для POI)

### 3.6. Переменные окружения

**Обязательные:**
- `BOT_TOKEN` — токен Telegram-бота
- `DATABASE_URL` — строка подключения к PostgreSQL
- `OPENWEATHER_API_KEY` — ключ OpenWeatherMap API
- `AMADEUS_API_KEY` — ключ Amadeus API
- `AMADEUS_API_SECRET` — секрет Amadeus API
- `FOURSQUARE_API_KEY` — ключ Foursquare API
- `OPENTRIPMAP_API_KEY` — ключ OpenTripMap API

**Опциональные:**
- `DEEPSEEK_API_KEY` — ключ DeepSeek API
- `DEEPSEEK_BASE_URL` — базовый URL DeepSeek API (по умолчанию: `https://api.deepseek.com`)

### 3.7. Кэширование

**Внутреннее кэширование (в памяти):**
- Погода: TTL = 600 секунд (10 минут)
- Достопримечательности: TTL = 1800 секунд (30 минут)
- Ключ кэша: кортеж с параметрами запроса

### 3.8. Обработка состояний (FSM)

**Состояния создания поездки (`TripCreation`):**
- `destination` — ввод направления
- `start_date` — ввод даты начала
- `end_date` — ввод даты окончания
- `notes` — ввод заметок

**Состояния создания задачи (`TaskCreation`):**
- `trip_selection` — выбор поездки
- `description` — ввод описания задачи

**Состояния выбора города (`CitySelection`):**
- `waiting_city_input` — ожидание ввода города
- Режимы: `weather`, `forecast`, `poi`

### 3.9. Логирование

- Уровень логирования: INFO
- Использование стандартного модуля `logging`
- Логирование ошибок API, успешных запросов, предупреждений

---

## 4. Пользовательский интерфейс

### 4.1. Команды бота

Список команд, отображаемых в меню Telegram:
- `/start` — 🚀 Запустить бота
- `/new_trip` — 🏝️ Создать новую поездку
- `/my_trips` — 🗺️ Мои поездки
- `/add_task` — 📋 Добавить задачу
- `/tasks` — ✅ Мои задачи
- `/weather` — 🌤️ Текущая погода
- `/forecast` — 📅 Прогноз погоды
- `/top_location` — 🏛️ Достопримечательности

### 4.2. Типы клавиатур

**Reply-клавиатуры:**
- Выбор поездки при добавлении задачи
- Выбор города из списка поездок
- Кнопка "Другой город..."

**Inline-клавиатуры:**
- Управление поездками (просмотр, задачи, удаление)
- Управление задачами (переключение статуса, удаление)

### 4.3. Форматирование сообщений

- Использование эмодзи для визуального оформления
- Структурированный вывод информации
- Форматирование дат: ДД.ММ.ГГГГ
- Единообразное отображение ошибок

---

## 5. ТРЕБОВАНИЯ К РАЗВЕРТЫВАНИЮ

### 5.1. Системные требования

- Python 3.9 или выше
- PostgreSQL 13+
- Docker и Docker Compose (для БД)
- Poetry (для управления зависимостями)

### 5.2. Процесс развертывания

1. Клонирование репозитория
2. Создание виртуального окружения
3. Установка зависимостей через Poetry
4. Создание файла `.env` с переменными окружения
5. Запуск PostgreSQL через Docker Compose
6. Выполнение миграций Alembic
7. Запуск бота: `python main.py`

### 5.3. Миграции БД

- Использование Alembic для управления схемой БД
- Автогенерация миграций на основе моделей SQLAlchemy
- Применение миграций: `alembic upgrade head`

---

## 6. БЕЗОПАСНОСТЬ

### 6.1. Хранение секретов
- Все секретные данные хранятся в переменных окружения
- Файл `.env` не должен попадать в систему контроля версий

### 6.2. Валидация данных
- Валидация всех пользовательских вводов
- Проверка формата дат
- Проверка длины строк
- Обработка некорректных данных

### 6.3. Обработка ошибок
- Глобальный обработчик ошибок
- Информативные сообщения для пользователя
- Логирование ошибок для отладки

---

## 7. ПРОИЗВОДИТЕЛЬНОСТЬ

### 7.1. Кэширование
- Кэширование результатов API-запросов
- Снижение нагрузки на внешние сервисы
- Улучшение скорости отклика

### 7.2. Асинхронность
- Асинхронная обработка всех запросов
- Неблокирующие HTTP-запросы
- Эффективное использование ресурсов

---

## 8. РАСШИРЯЕМОСТЬ

### 8.1. Архитектурные решения
- Модульная структура кода
- Разделение ответственности (SRP)
- Использование репозиториев для абстракции БД
- Сервисный слой для бизнес-логики

### 8.2. Возможности для расширения
- Добавление новых источников данных для POI
- Интеграция с другими API (отели, рестораны)
- Добавление уведомлений о предстоящих поездках
- Экспорт данных о поездках
- Многопользовательские поездки

---

## 9. ТЕСТИРОВАНИЕ

### 9.1. Типы тестирования
- Unit-тесты для сервисов и репозиториев
- Интеграционные тесты для обработчиков
- Тесты валидации данных

### 9.2. Моки
- Мокирование внешних API
- Тестовые данные для БД

---

## 10. ДОКУМЕНТАЦИЯ

### 10.1. README
- Инструкции по установке
- Описание переменных окружения
- Примеры использования

---

## 11. ОГРАНИЧЕНИЯ И ОСОБЕННОСТИ

### 11.1. Ограничения
- Кэширование только в памяти (не сохраняется при перезапуске)
- Максимум 50 результатов Foursquare API за запрос
- Прогноз погоды только на 5 дней
- По умолчанию 5 достопримечательностей

### 11.2. Особенности реализации
- Fallback-механизм для получения POI (4 уровня приоритета)
- Использование FSM для многошаговых процессов
- Автоматическая регистрация пользователей
- Интеграция с LLM (DeepSeek) для генерации POI

---

## 12. ВЕРСИОНИРОВАНИЕ

- Версия проекта: 0.1.0
- Система управления зависимостями: Poetry
- Миграции БД: Alembic

---

**Дата создания ТЗ:** 2025
**Версия ТЗ:** 1.0
**Статус:** Реализовано
